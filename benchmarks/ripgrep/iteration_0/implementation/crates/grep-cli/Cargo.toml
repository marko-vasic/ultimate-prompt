[package]
name = "grep-cli"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "CLI utilities for grep-like tools"

[dependencies]
bstr = { workspace = true }
log = { workspace = true }
termcolor = { workspace = true }
